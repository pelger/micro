pipeline {
  agent any 

  stages {
    stage('Checkout') {
      steps { 
        checkout scm
      }
    }
    stage('Build') { 
      steps { 
        sh 'set'
        sh 'npm install'
      }
    }
    stage('Test'){
      steps {
        sh 'npm test'
      }
    }
  }
}

